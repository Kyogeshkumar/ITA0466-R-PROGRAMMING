# Define a sample vector
vector <- c(10, 25, 6, 48, 12, 35, 9, 22)

# Find the maximum value
max_value <- max(vector)

# Find the minimum value
min_value <- min(vector)

# Print the results
cat("Maximum value:", max_value, "\n")
cat("Minimum value:", min_value, "\n")
